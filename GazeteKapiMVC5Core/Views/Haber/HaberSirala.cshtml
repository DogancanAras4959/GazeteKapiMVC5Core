@model IEnumerable<GazeteKapiMVC5Core.Models.News.NewsModel.NewsLıstItemModel>
@{
    ViewData["Title"] = "HaberSirala";
    Layout = "~/Views/Shared/_Layout.cshtml";

    int rowNo = 0;
}

<style>
    #sortable tr:hover {
        background-color: #0094ff;
        color: #fff;
    }
</style>

<link href="https://uploads.gazetekapi.com/admin/wwwroot/assets/css/pageline.css" rel="stylesheet" />

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="row mb-3">
                    <div class="col-sm-12">
                        <h4 class="card-title">Manşet Haberleri Sırala</h4>
                        <p class="card-title-desc">Ana Sayfanızdaki manşet bölümünüz için haberlerinizi sıralayın</p>
                    </div>

                </div>
                <div class="table-rep-plugin">
                    <div class="table-responsive mb-0" data-bs-pattern="priority-columns">
                        <table class="table table-bordered dt-responsive nowrap" style="border-collapse: collapse; border-spacing:0; width: 50%;">
                            <thead>
                                <tr>
                                    <th style="text-align:center;">Sıra</th>
                                    <th>Başlık</th>
                                    @*<th>İşlem</th>*@
                                </tr>
                            </thead>
                            <tbody id="sortable" style="cursor:pointer;">
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td style="text-align:center;">@Html.DisplayFor(model => item.RowNo)</td>
                                        <td class="taskSingleInline" id="task@(item.Id)" data-taskid="@(item.Id)">
                                            <a asp-action="HaberDuzenle" asp-controller="Haber" asp-route-id="@item.Id">@Html.DisplayFor(modelItem => item.Title)</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-12">
                        <a class="btn btn-primary" href="@Url.Action("HaberSirala","Haber")">Sayfayı Güncelle</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    var sirala = function (Id) {

        document.getElementById("edit").style.display = "none";
        document.getElementById("process").style.display = "block";

        var sira = $("#" + Id + "_siraInput").val();
        var id = Id;

        $.ajax({
            type: "POST",
            url: "/Haber/siraDegistir",
            data: { id: id, sira: sira },
            success: function (response) {
                window.location.href = '@Url.Action("HaberSirala","Haber")';

            }
        });
    }
</script>



<script>

    $(function () {
        $("#sortable").sortable({
            update: function (event, ui) {
                var itemIds = "";
                $("#sortable").find(".taskSingleInline").each(function () {
                    var itemId = $(this).attr("data-taskid");
                    itemIds = itemIds + itemId + ",";
                });
                $.ajax({

                    url: '@Url.Action("SiralamayiGuncelle","Haber")',
                    data: { itemIds: itemIds },
                    type: 'POST',
                    succcess: function (data) {
                        window.location.href = '@Url.Action("HaberSirala","Haber")';
                    },
                    error: function (xhr, status, error) {
                        window.location.href = '@Url.Action("HaberSirala","Haber")';
                    }

                });
            }
        });
    });

</script>